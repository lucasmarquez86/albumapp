'use client'
import Link from 'next/link'
import {usePathname} from 'next/navigation'
import {LayoutDashboard,Library,Search,UserCheck,Plus} from 'lucide-react'
const nav=[['/dashboard','Inicio',LayoutDashboard],['/dashboard/mis-albumes','Mis álbumes',Library],['/dashboard/seleccionar-album','Seleccionar álbum',Search],['/dashboard/solicitudes','Solicitudes',UserCheck]] as const
export default function DashboardShell({children}:{children:React.ReactNode}){const path=usePathname();return <div className="min-h-screen flex bg-[#f7f7f7]"><aside className="hidden md:flex w-64 bg-white border-r flex-col"><div className="p-6 border-b"><Link href="/dashboard" className="text-xl font-black">ALBUMAPP</Link><p className="text-xs text-gray-400 mt-1">Tu espacio</p></div><nav className="p-3 flex-1">{nav.map(([href,label,Icon])=><Link key={href} href={href} className={`flex items-center gap-3 px-4 py-3 rounded-xl text-sm mb-1 ${path===href?'bg-black text-white':'hover:bg-gray-100 text-gray-600'}`}><Icon size={17}/>{label}</Link>)}</nav><div className="p-4"><Link href="/dashboard/crear-album" className="flex items-center justify-center gap-2 bg-black text-white rounded-xl p-3 text-sm font-bold"><Plus size={17}/>Crear álbum</Link></div></aside><div className="flex-1 min-w-0"><header className="md:hidden bg-white border-b p-4 flex justify-between"><b>ALBUMAPP</b><Link href="/dashboard/mis-albumes" className="text-sm">Mis álbumes</Link></header><main className="p-5 md:p-10">{children}</main></div></div>}
