'use client'
import {useState} from 'react'
import {Search,Check,Clock} from 'lucide-react'
export default function Page(){const [searched,setSearched]=useState(false);const [requested,setRequested]=useState(false);return <div className="max-w-2xl mx-auto"><h1 className="text-4xl font-black">Seleccionar álbum</h1><p className="text-gray-500 mt-2 mb-7">Buscá por nombre o usá el código que te pasó el administrador.</p><div className="bg-white border rounded-3xl p-5"><label className="text-sm font-bold">Nombre o código</label><div className="mt-2 space-y-2 sm:space-y-0 sm:grid sm:grid-cols-[1fr_auto] sm:gap-2"><div className="relative min-w-0"><Search className="absolute left-4 top-1/2 -translate-y-1/2 text-gray-400 pointer-events-none" size={18}/><input className="input !pl-12" placeholder="Ej. LP2015"/></div><button onClick={()=>setSearched(true)} className="w-full sm:w-auto bg-black text-white rounded-xl px-5 py-3 font-bold">Buscar</button></div>{searched&&<div className="mt-6 border-2 border-black rounded-2xl overflow-hidden"><div className="bg-black text-white p-5 flex gap-4 items-center"><div className="w-16 h-16 shrink-0 rounded-full border border-white/30 grid place-items-center text-xs">ESCUDO</div><div className="min-w-0"><small className="text-white/50">ÁLBUM ENCONTRADO</small><h2 className="text-xl font-black">Los Pibes 2015</h2><p className="text-white/60 text-sm">Temporada 2026 · LP2015</p></div></div><div className="p-5">{!requested?<><p className="text-sm text-gray-600 mb-4">El administrador debe aprobarte antes de que puedas comenzar a coleccionar este álbum.</p><button onClick={()=>setRequested(true)} className="w-full bg-black text-white rounded-xl py-4 font-bold">Solicitar acceso</button></>:<div className="text-center py-2"><Clock className="mx-auto"/><h3 className="font-black text-lg mt-3">Solicitud enviada</h3><p className="text-gray-500 text-sm mt-1">Te avisaremos cuando el administrador la acepte.</p><div className="mt-4 bg-gray-100 rounded-xl p-3 text-sm font-bold">Esperando aprobación</div></div>}</div></div>}</div><div className="mt-6 flex gap-3 text-sm text-gray-500"><Check size={18} className="shrink-0"/><p>Cuando te acepten, el álbum aparecerá automáticamente en <b className="text-black">Mis álbumes</b>.</p></div></div>}
