'use client'
import {useEffect,useState} from 'react'
import Link from 'next/link'
type Card={id:number,name:string}
const cards:Card[]=Array.from({length:52},(_,i)=>({id:i+1,name:['JUAN','TOMI','FEDE','MATI','NICO','LUCA'][i%6]}))
export default function Album(){
 const [page,setPage]=useState(0),[owned,setOwned]=useState<number[]>(Array.from({length:37},(_,i)=>i+1)),[selected,setSelected]=useState<Card|null>(null),[portrait,setPortrait]=useState(false),[tab,setTab]=useState<'album'|'sobre'|'figus'>('album'),[pack,setPack]=useState<Card[]>([]),[duplicates,setDuplicates]=useState<Record<number,number>>({2:2,8:1,14:3,25:1}),[filterMissing,setFilterMissing]=useState(false)
 useEffect(()=>{const check=()=>setPortrait(innerHeight>innerWidth&&innerWidth<900);check();addEventListener('resize',check);return()=>removeEventListener('resize',check)},[])
 async function landscape(){try{await document.documentElement.requestFullscreen?.();await (screen.orientation as any).lock?.('landscape')}catch{}}
 function downloadJpg(c:Card){const canvas=document.createElement('canvas');canvas.width=900;canvas.height=1200;const x=canvas.getContext('2d')!;x.fillStyle='#fff';x.fillRect(0,0,900,1200);x.strokeStyle='#000';x.lineWidth=20;x.strokeRect(25,25,850,1150);x.fillStyle='#111';x.font='bold 42px Arial';x.fillText('#'+String(c.id).padStart(3,'0'),70,100);x.fillStyle='#eee';x.beginPath();x.arc(450,500,220,0,Math.PI*2);x.fill();x.fillStyle='#111';x.font='150px Arial';x.textAlign='center';x.fillText('⚽',450,555);x.font='bold 85px Arial';x.fillText(c.name,450,930);x.font='36px Arial';x.fillText('LOS PIBES FC',450,1000);const a=document.createElement('a');a.href=canvas.toDataURL('image/jpeg',.94);a.download=`figurita-${c.id}-${c.name}.jpg`;a.click()}
 const pct=Math.round(owned.length/cards.length*100),missing=cards.length-owned.length,maxPage=Math.ceil(cards.length/12)-1,visible=cards.slice(page*12,page*12+12)
 const openPack=()=>setPack([...cards].sort(()=>Math.random()-.5).slice(0,3));
 const pastePack=()=>{const n=[...owned],d={...duplicates};pack.forEach(c=>{if(n.includes(c.id))d[c.id]=(d[c.id]||0)+1;else n.push(c.id)});setOwned(n);setDuplicates(d);setPack([]);setTab('album')}
 const Slot=({c}:{c:Card})=>{const yes=owned.includes(c.id);if(filterMissing&&yes)return <div/>;return <button onClick={()=>yes&&setSelected(c)} className={`relative min-h-0 rounded-md border-2 overflow-hidden ${yes?'bg-zinc-100 text-black border-white':'bg-zinc-700/70 text-zinc-400 border-zinc-400'}`}><span className="absolute top-1 left-1.5 text-[10px] font-black">{String(c.id).padStart(2,'0')}</span>{yes?<div className="h-full flex flex-col justify-end"><div className="flex-1 grid place-items-center text-3xl">⚽</div><b className="bg-black text-white text-[9px] py-1">{c.name}</b></div>:<div className="h-full grid place-items-center"><div className="relative w-10 h-12 opacity-70"><div className="absolute top-0 left-1/2 -translate-x-1/2 w-5 h-5 rounded-full bg-current"/><div className="absolute bottom-0 left-1/2 -translate-x-1/2 w-10 h-8 rounded-t-full bg-current"/></div></div>}</button>}
 return <main className="fixed inset-0 bg-[#080808] text-white overflow-hidden">{portrait&&<div className="fixed inset-0 z-[60] bg-black grid place-items-center text-center p-8"><div><div className="text-6xl">↻</div><h2 className="text-3xl font-black mt-4">GIRÁ EL CELULAR</h2><p className="text-white/60 mt-2">Esta pantalla funciona únicamente en horizontal.</p><button onClick={landscape} className="mt-6 bg-white text-black px-6 py-4 rounded-xl font-black">VER EN HORIZONTAL</button></div></div>}{selected&&<div onClick={()=>setSelected(null)} className="fixed inset-0 z-50 bg-black/95 grid place-items-center p-2"><div onClick={e=>e.stopPropagation()} className="h-full max-h-[390px] flex items-center gap-3"><div className="h-full aspect-[3/4] bg-white text-black rounded-xl p-3 flex flex-col"><small>#{selected.id}</small><div className="text-6xl grid place-items-center flex-1">⚽</div><h2 className="text-2xl font-black text-center">{selected.name}</h2><p className="text-xs text-center">LOS PIBES FC</p></div><div className="w-28 space-y-2"><button onClick={()=>downloadJpg(selected)} className="w-full bg-white text-black rounded-xl py-4 text-xs font-bold">↓ JPG</button><button onClick={()=>setSelected(null)} className="w-full border border-white/30 rounded-xl py-4 text-xs font-bold">✕ Cerrar</button></div></div></div>}<div className="h-full flex flex-col p-2 md:p-3"><header className="h-[52px] shrink-0 grid grid-cols-[1fr_auto_1fr] items-center border-b border-white/10 px-2"><div className="flex items-center gap-3"><Link href="/dashboard/mis-albumes" className="text-3xl">‹</Link><b>ÁLBUM</b><span className="text-white/40">|</span><span>Los Pibes FC</span></div><div className="w-14 h-14 rounded-b-xl border-2 border-white bg-black grid place-items-center font-black z-10">LP</div><nav className="justify-self-end flex bg-white/5 border border-white/10 rounded-xl p-1">{(['album','sobre','figus'] as const).map(t=><button key={t} onClick={()=>setTab(t)} className={`px-4 md:px-6 py-2 rounded-lg uppercase text-[10px] md:text-xs font-black ${tab===t?'bg-white text-black':''}`}>{t==='sobre'?'SOBRES':t}</button>)}</nav></header>{tab==='album'&&<div className="flex-1 min-h-0 flex gap-2 pt-2"><aside className="w-[22%] max-w-[220px] shrink-0 rounded-xl border border-white/15 bg-zinc-950 p-3 flex flex-col justify-center"><div className="flex justify-between"><b className="text-xl md:text-2xl">{owned.length} / {cards.length}</b><b className="text-lg md:text-xl">{pct}%</b></div><small>Figuritas pegadas</small><div className="h-2 bg-zinc-700 rounded-full mt-3 overflow-hidden"><div className="h-full bg-white" style={{width:`${pct}%`}}/></div><div className="flex gap-3 text-[10px] md:text-xs mt-3"><span>Faltan: {missing}</span><span>|</span><span>Repetidas: {Object.values(duplicates).reduce((a,b)=>a+b,0)}</span></div></aside><section className="relative flex-1 min-w-0 rounded-xl border border-white/20 bg-gradient-to-b from-zinc-800 via-zinc-950 to-black p-2 grid grid-cols-[1fr_28%_1fr] gap-2 overflow-hidden"><button disabled={!page} onClick={()=>setPage(p=>p-1)} className="absolute left-1 top-1/2 -translate-y-1/2 z-20 text-4xl disabled:opacity-20">‹</button><div className="grid grid-cols-3 grid-rows-2 gap-1.5 pl-5 min-h-0">{visible.slice(0,6).map(c=><Slot key={c.id} c={c}/>)}</div><div className="border-x border-white/10 grid place-items-center text-center"><div><h2 className="font-black">LOS PIBES FC</h2><div className="w-20 h-20 rounded-full border-2 border-white mx-auto my-3 grid place-items-center font-black text-2xl">LP</div><b className="text-xs">TEMPORADA 2026</b><small className="block text-white/40">Página {page+1} de {maxPage+1}</small></div></div><div className="grid grid-cols-3 grid-rows-2 gap-1.5 pr-5 min-h-0">{visible.slice(6,12).map(c=><Slot key={c.id} c={c}/>)}</div><button disabled={page>=maxPage} onClick={()=>setPage(p=>p+1)} className="absolute right-1 top-1/2 -translate-y-1/2 z-20 text-4xl disabled:opacity-20">›</button></section></div>}{tab==='sobre'&&<div className="flex-1 min-h-0 grid place-items-center">{!pack.length?<div className="flex items-center gap-8"><div className="w-32 h-44 border-2 border-white rounded-xl grid place-items-center text-3xl font-black">LP</div><div><small className="text-white/50">NUEVO SOBRE</small><h2 className="text-3xl font-black">3 FIGURITAS</h2><p className="text-white/50 mt-2">Próximo sobre en 5h 42m</p><button onClick={openPack} className="mt-4 bg-white text-black px-7 py-3 rounded-xl font-black">ABRIR SOBRE</button></div></div>:<div className="w-full max-w-3xl text-center"><h2 className="text-xl font-black mb-3">ESTAS SON TUS FIGUS</h2><div className="grid grid-cols-3 gap-3 h-[55vh]">{pack.map(c=><button onClick={()=>setSelected(c)} key={c.id} className="bg-white text-black rounded-xl p-3 flex flex-col"><small className="text-left">#{c.id}</small><div className="text-5xl grid place-items-center flex-1">⚽</div><b>{c.name}</b><span className="text-[10px] font-black mt-1">{owned.includes(c.id)?'REPETIDA':'NUEVA'}</span></button>)}</div><button onClick={pastePack} className="mt-2 bg-white text-black px-7 py-2 rounded-xl font-black">PEGAR LAS NUEVAS</button></div>}</div>}{tab==='figus'&&<div className="flex-1 min-h-0 p-3 overflow-y-auto"><div className="flex justify-between items-end"><div><h2 className="text-2xl font-black">REPETIDAS</h2><p className="text-white/50 text-xs">Las que podés guardar para futuros intercambios.</p></div><button disabled className="border border-white/20 rounded-xl px-4 py-2 text-xs opacity-40">INTERCAMBIAR · Próximamente</button></div><div className="grid grid-cols-6 gap-2 mt-4">{Object.entries(duplicates).map(([id,count])=>{const c=cards[Number(id)-1];return <button onClick={()=>setSelected(c)} key={id} className="bg-white text-black rounded-lg aspect-[3/4] p-2 flex flex-col"><small className="text-left">#{id}</small><div className="text-3xl grid place-items-center flex-1">⚽</div><b className="text-[10px]">{c.name}</b><span className="text-[9px] font-black">x{count}</span></button>})}</div></div>}<footer className="h-[48px] shrink-0 mt-2 border-t border-white/10 flex items-center justify-between px-3"><span className="text-[10px]">ⓘ Tocá una figurita para verla más grande</span><button onClick={()=>{setFilterMissing(!filterMissing);setTab('album')}} className={`border rounded-xl px-5 py-2 text-xs ${filterMissing?'bg-white text-black':'border-white/15'}`}>▦ {filterMissing?'Ver todas':'Ver faltantes'}</button><button className="border border-white/15 rounded-xl px-5 py-2 text-xs">▽ Filtrar</button></footer></div></main>}
